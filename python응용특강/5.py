"""
- 관련 있는 함수를 모아놓은 집합을 모듈이라고 한다.

< 모듈의 종류 >
- 표준 모듈 : 파이썬에서 제공하는 모듈
- 사용자 정의 모듈
- 서드 파티듈(3rd Party) 모듈
"""

# import math
# print(dir(math))

"""
- 패키지는 특정 주제로 모듈을 모아서 분리해 놓은 것이다. 
- 폴더의 형태로 나타냄 
from 패키지명.모듈명 import 함수명
"""


"""
- 객체지향 
class 클래스명:
    # 이 부분에 관련 코드 구현 

- 파이썬 메서드의 첫 번째 인자로 항상 인스턴스가 전달된다.
- 클래스 내에 정의된 self는 클래스 인스턴스
- 메서드의 self로 전달되는 것은 인스턴스 자체이다. 
- 호출할 때는 self 자리에 아무것도 넘겨주지 않는 이유는 첫 번째 인자인 self에 대한 값은 파이썬이 자동으로 넘겨주기 때문이다. 

"""